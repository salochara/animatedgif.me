<p id="notice"><%= notice %></p>

<p>
  <strong>Image data:</strong>
  <%= image_tag @gif.image_url %>
</p>

<p>Uploaded by:</p>
<p>
  <strong>User:</strong>
  <p>hey</p>
  <%=  @gif.user.email %>
</p>

<div>
  <% @gif.tag_list.each do |tag_name| %>
    <%= link_to tag_name, root_path(tag: tag_name) %>
  <% end %>

</div>



<%= link_to 'Edit', edit_gif_path(@gif) if current_user == @gif.user %> |
<%= link_to 'Back', gifs_path %>
