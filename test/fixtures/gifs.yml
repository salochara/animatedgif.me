# Read about fixtures at https://api.rubyonrails.org/classes/ActiveRecord/FixtureSet.html

# necessary for tests as a string in image data
one:
 user: one
 image_data: '<%= TestData.image_data %>'

two:
 user: two
 image_data: '<%= TestData.image_data %>'
